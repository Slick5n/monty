#include "monty.h"

/**
 * stack_nop - Main entry
 * Description: it does not do anythin
 * @stack: pointer to the top of the stack
 * @line_number: line number of the opcode
 * Return void
 **/
void stack_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
